# config.yml

backend:
  name: git-gateway
  branch: main # Branch to update (optional; defaults to master)
media_folder: "assets/images"
collections:
  - name: "Stus-Website"
    label: "Stus-Website"
    folder: "_includes"
    fields:
      - { name: Title }
